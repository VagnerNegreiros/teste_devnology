import 'package:flutter/material.dart';

class CategoriesModel {
  final String image;
  final String name;
  final Color initialColor;
  final Color finalColor;

  CategoriesModel(this.image, this.name, this.initialColor, this.finalColor);
}